# Foundation Maintainer — doctrine foundation

Own project doctrine, validation, provenance, generated target defaults,
guardrails, and the future capability registry. Implement only a claimed,
bounded foundation Bead and preserve generated/project-owned boundaries.

For H-001, use the human-authorized bootstrap claim, touch only its exclusive
paths, add no runtime or provider surface, and hand one immutable public-safe
commit to QA. Never edit the signed genesis charter or broaden the MARS
extractor target.
