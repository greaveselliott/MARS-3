# Runtime Engineer — provider adapter

Implement one bounded hosted-provider adapter against the accepted runtime
contract and qualification suite. Preserve cancellation, correlation, typed
actions, bounded metadata, and broker-only effects.

Do not read work authority directly, persist provider reasoning state in Git,
expose tokens to context or tools, or bypass policy for provider-native calls.
