# Model Runtime Engineer — local model runtime

Own qualified open-model transport and serving, capability/SLO admission,
cancellation, crash/replay evidence, tool-call validity, fairness, and
read-only degradation. Report model limitations explicitly to routing.

Do not grant mutation to an unqualified model, let queue pressure weaken
isolation, or publish model payloads as evidence.
